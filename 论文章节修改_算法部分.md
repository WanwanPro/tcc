# 论文章节修改建议（算法部分）

以下是根据你的代码实际实现修改后的论文章节，主要调整 3.1、4.1~4.4，使其与代码保持一致。

---

## 3.1 停车场场景建模

把现实停车场环境抽象成数学模型，要把停车场内主要元素进行形式化表示，包括车位、通道、障碍物等内容。车位可以建成为二维坐标系里的离散点或区域[8]，每个车位具有唯一标识符，标识符和对应几何位置信息绑定。通道供车辆行驶，可以表示成连接各个车位的线段集合，线段集合构成停车场内部的可行路径网络。障碍物主要包括立柱、墙壁、其他不可通行的区域，建模时将这些区域归为禁止通行的节点或区域，路径规划算法在运行过程中会直接排除这类区域。通过这种建模方式，停车场环境可以转化为包括节点和边的图结构，从而支撑后续动态加权A*算法的设计。该建模方法会考虑停车场内部设施的分布特性，车位既可能呈块状分布，也可能呈链状分布，分布状态会影响算法遍历节点的方式选择。

在具体实现阶段，停车场场景建模需要结合传感器数据[9]。超声波传感器和监控设备可以检测车位占用状态，系统把收集到的信息传回中心服务器进行处理。实时数据采集接入建模流程，停车场的变化会得到动态呈现，路径规划和车位分配策略的准确程度也会得到提高[10]。系统后端以 `MapNode` 模型存储地图节点（包含节点ID、类型、坐标、楼层、连接关系等字段），以 `ParkingSpace` 模型存储车位信息（包含车位ID、楼层、区域、类型、状态、坐标等字段），二者通过 `nodeId` 进行关联。最终形成的数学模型可以完整描述停车场静态布局，并且涵盖停车场动态使用状态，支持智能停车场系统对运行进行优化。

---

## 4 核心算法设计与实现

### 4.1 传统A*算法基础

传统A*算法是一种广泛应用于路径规划领域的启发式搜索算法，其核心思想是通过结合起始节点到当前节点的实际代价与当前节点到目标节点的估计代价来实现高效搜索。在A*算法中，估价函数 $f(n)$ 由两部分组成：从起始节点到当前节点 $n$ 的实际代价 $g(n)$，以及从当前节点 $n$ 到目标节点的估计代价 $h(n)$，即：

$$f(n) = g(n) + h(n)$$

其中，$h(n)$ 通常采用曼哈顿距离或欧几里得距离作为启发函数，用于指导搜索方向。为了管理搜索过程中的节点，A*算法引入了开放列表（Open List）和关闭列表（Closed List）两个数据结构。开放列表用于存储待扩展的节点，而关闭列表则记录已扩展且不再需要考虑的节点。算法的搜索过程可概括为以下步骤：首先，将起始节点加入开放列表；然后，从开放列表中选择 $f(n)$ 值最小的节点作为当前节点进行扩展；对于当前节点的每个邻居节点，计算其 $f(n)$ 值并判断是否已存在于开放列表或关闭列表中；若不存在，则将其加入开放列表，否则更新其 $f(n)$ 值并重新计算父节点指针；最后，当目标节点被加入开放列表或扩展时，算法终止，并通过回溯父节点指针生成最终路径。

尽管A*算法在全局路径规划中具有显著优势，但其性能仍受制于启发函数的设计。当 $h(n) < h^*(n)$ 时，算法能够保证找到全局最优路径，但可能导致搜索节点过多，降低搜索效率；当 $h(n) = h^*(n)$ 时，算法严格按照最短距离搜索，效率较高；当 $h(n) > h^*(n)$ 时，算法搜索节点较少，但容易陷入局部收敛，无法得到全局最优解。因此，在实际应用中，如何设计合理的启发函数以平衡搜索效率与路径最优性成为关键问题。

---

### 4.2 动态权重与代价模型设计

本文发现传统A*算法用于停车场导航时，在路径平滑、转弯次数控制、对复杂环境适应能力等方面存在不足[11]，为此对一种动态加权A*路径规划方法进行了改进。本文在传统A*算法评价函数中引入动态权重机制，使算法在搜索前期可以适当增加启发式函数的影响，进而提高搜索速度；在接近目标节点时，逐渐降低权重，使路径结果更加合理。算法运用该方法在搜索效率和路径优化上可以达到较好平衡，更适合停车场内部道路结构复杂、车辆通行条件多变的现实场景。

本文的动态权重模型将评价函数改进为：

$$f(n) = g(n) + w(p) \times h(n)$$

其中 $w(p)$ 为随搜索进度 $p$ 动态变化的权重系数。本文在代码中实现了多种权重衰减策略，包括指数衰减策略（$w(p) = w_{min} + (w_{max} - w_{min}) \times e^{-kp}$）、线性衰减策略、Sigmoid平滑过渡策略、阶梯式策略以及自适应策略。默认采用指数衰减策略，设置 $w_{max}=2.5$、$w_{min}=1.0$、$k=3.5$，使搜索初期权重较高以快速逼近目标，后期权重趋近于1以保证路径质量。

在代价模型设计方面，本文结合停车场导航需求，在传统距离代价基础上增加了多种场景化惩罚项，使生成的路径更贴合车辆实际行驶需求。具体包括：

（1）**转弯惩罚**：当行驶方向发生改变时施加惩罚，其中掉头行为的惩罚系数为普通转弯的1.5倍；
（2）**局部环境惩罚**：检测当前节点周围8邻域的通行状况，对狭窄通道（周围被阻挡比例高）、已占用车位缓冲区域、死胡同（可通行方向少于等于2个）分别施加惩罚；
（3）**方向偏离惩罚**：当移动方向与目标主方向明显背离时施加轻微惩罚，提升路径稳定性但不过度限制必要绕行；
（4）**过早转弯惩罚**：若在当前位置转弯会导致后续仍需长距离调整方向，则根据与目标点的坐标差距施加递增惩罚；
（5）**车道偏好调整**：检测前进方向的视野开阔度以及左右两侧的通行空间平衡性，对贴近边缘或视野受限的路径施加惩罚，对视野开阔的路径给予奖励。

上述代价模型通过 `useEnhancedCostModel` 开关控制启用，为后续系统实现和实验分析奠定算法基础。

---

### 4.3 算法实现与路径优化

传统A*算法通常使用数组或未建立索引的普通堆结构来实现开放列表，该方法在进行节点查找和更新时效率较低。本文设计了使用二叉最小堆的开放列表优化结构 `OptimizedBinaryHeap`，以提高路径搜索效率。该结构主要在三个方面进行了改进：首先是预分配内存机制，在初始化阶段预先分配固定大小的数组空间（默认10000个节点容量），以减少动态扩容造成的额外开销；二是节点索引映射方式，利用 `Map` 结构保存节点键值和堆中位置的对应关系，由此把任意节点的查找复杂度降低到 $O(1)$；三是节点更新方式，搜索中找到更好的路径时，能够直接定位并更新堆里已有的节点，然后通过上浮（`_bubbleUp`）或下沉（`_sinkDown`）操作完成重新排序，更新复杂度为 $O(\log n)$。该结构与传统实现相比，在节点查找和更新执行上拥有更快的效率。

表4.1给出了不同数据结构的操作复杂度对比。

| 操作类型 | 数组实现 | 未建立索引的普通堆 | 优化二叉最小堆 |
|---------|---------|----------------|-------------|
| 获取最小节点 | $O(n)$ | $O(1)$ | $O(1)$ |
| 插入节点 | $O(1)$ | $O(\log n)$ | $O(\log n)$ |
| 删除最小节点 | $O(n)$ | $O(\log n)$ | $O(\log n)$ |
| 查找任意节点 | $O(n)$ | $O(n)$ | $O(1)$ |

启发式函数的选择会直接影响A*算法的搜索效率和路径质量。本文在 `Heuristics` 模块中实现了曼哈顿距离、欧几里得距离、切比雪夫距离、对角线距离以及加权曼哈顿距离等多种启发式函数。其中，曼哈顿距离适用于规则道路上的四方向移动场景，计算复杂度为 $O(1)$；欧几里得距离适用于连续空间中的直线距离估计；切比雪夫距离和对角线距离适用于八方向搜索场景；加权曼哈顿距离可以按照方向或场景特点进行适当调整。在本文形成的停车场道路模式中，车辆主要按照道路连通的方位行驶，因此选择曼哈顿距离作为默认启发式函数。

因为A*算法生成的原始路径里可能有冗余节点，使路径不够平滑，本文设计了 `PathOptimizer` 路径优化器来处理搜索结果。路径优化主要分为两个步骤：首先是进行冗余消除，依次检查路径上的连续节点，当发现相邻的三个节点位于同一条直线（通过向量叉积判断方向是否一致）时，删除中间的节点，减少不必要的拐点。路径平滑使用拉绳算法进行视线检测，基于Bresenham直线算法判断从当前节点是否可以直达前方较远的节点，然后用直线连接可视的最远节点，减少路径里多余的转折，让导航路径更加平滑，提高可行驶性[12]。优化器还提供 `evaluatePath` 方法对路径质量进行评估，统计路径长度和转弯次数，为算法性能对比提供量化依据。

为了实现不同路径规划算法间灵活的切换和性能比较，本文设计了 `PathfindingManager` 路径规划算法管理器来统一管理动态加权A*算法、标准A*和快速A*三种算法。系统利用 `setCurrent` 方法切换当前算法，通过 `findPath` 方法执行路径搜索，利用 `compare` 方法分析和对比不同算法的性能。管理器会在搜索过程中记录统计信息，包括扩展节点数、访问节点数、路径长度、计算时间、权重变化次数、平均权重、转弯次数等指标，为后续的算法调优和性能分析提供量化依据。

三种算法的配置差异如下：
- **动态加权A***：采用指数衰减策略，$w_{max}=2.5$，$w_{min}=1.0$，启用路径平滑和增强代价模型；
- **标准A***：采用固定权重1.0，关闭路径平滑和增强代价模型，仅基于基础距离代价搜索；
- **快速A***：采用线性衰减策略，$w_{max}=3.0$，$w_{min}=1.5$，关闭路径平滑和增强代价模型，以速度为优先。

---

### 4.4 车位推荐与导航协同策略

当前系统并未将车位分配抽象为独立的大规模分区调度模型，而是将推荐逻辑作为导航前置决策的一部分，与路径规划协同实现。后端首先依据停车场编号、车位状态和车辆类型筛选可用车位；当用户未显式提供当前位置时，系统以入口节点作为默认起点，从而保证推荐流程可以与实际导航入口保持一致。

在推荐评分阶段，系统主要综合考虑距离、楼层差异和用户偏好三个维度。距离评分用于衡量起点到候选车位的接近程度，采用二维平面欧几里得距离计算，并按最大参考距离（500单位）归一化为0~100分，距离越近分数越高。楼层评分用于反映跨层停放带来的额外成本，同层得100分，每跨一层扣减20分。偏好评分则依据多项条件进行修正：靠近出口区域加20分，位于首层加15分，邻近电梯加10分；若用户偏好无障碍车位或新能源车位且候选车位类型匹配，则各加25分。最终采用加权求和方式形成综合得分，权重分配为距离40%、楼层20%、偏好40%，并按得分高低输出候选结果。

在导航联动方面，推荐模块输出的目标车位会作为小程序路径规划页面的终点输入，前端再调用 `PathfindingManager` 执行动态加权A*、标准A*和快速A*之间的切换与比较。这样，系统形成了"空闲车位筛选—推荐目标确定—路径规划计算—前端可视化导航"的完整链路。将车位推荐逻辑并入核心算法章节后，论文结构更加紧凑，也能够更加准确地反映当前系统的真实实现重点。

---
