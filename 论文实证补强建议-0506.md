# 论文实证补强建议（基于当前 docx 与 tcc 代码）

## 一、当前论文主要问题

当前 docx 的第 6 章已经包含“实验环境、实验方案、性能分析与对比”，但实证链条仍偏弱，主要体现在：

1. 缺少明确的测试执行证据。论文写了实验结果，但没有给出实际运行的脚本名称、执行命令、测试顺序和控制台输出摘要，老师容易认为只是“测试设计”。
2. 数据口径不一致。第 6.1.3 写“导航节点 20 个、导航边 35 条”，但当前代码实际数据为导航节点 47 个、边 54 条；地图要素总数为 193，其中车位 168 个。
3. 系统性能表缺少可复现来源。表 6.4 写了接口平均响应时间、P95、P99，但当前本机运行 `backend/tests/performance.test.js` 未通过，原因是 `backend/controllers/spaceController.js` 第 5 行引用 `../../shared/dal/atomic` 时 Jest 未解析到 `.js` 后缀文件。因此表 6.4 若继续保留，应补充实际压测命令或改为“测试设计与待验证项”，否则容易被质疑。
4. “路径质量对比”是较强实证点，但缺少完整表格。当前脚本 `scripts/test-dynamic-pathfinding.js` 已经能输出标准 A* 与动态加权 A* 的对比结果，应把该表直接放入论文。
5. 第 6.3.1 中的 5 个场景表更像手工整理数据，而不是当前代码直接输出结果。建议保留场景设计，但实证重点转向当前可运行脚本的真实输出。

## 二、建议直接补入第 6 章的实证结构

建议将第 6 章调整为以下结构：

- 6.1 实验环境与数据来源
- 6.2 测试流程与复现方法
- 6.3 真实项目数据验证
- 6.4 动态加权 A* 对比实验
- 6.5 单元测试与系统测试
- 6.6 实验结果分析

这样能把“设计了什么测试”变成“用什么数据、执行什么命令、得到什么输出、说明什么问题”。

## 三、可直接粘贴到论文的正文补充

### 6.2 测试流程与复现方法（建议新增）

为避免实验仅停留在方案设计层面，本文结合系统代码对核心功能进行了可复现实证测试。测试过程按照“数据校验-路径规划验证-算法对比-单元测试”的顺序执行，具体步骤如下：

（1）执行导航系统验证脚本，检查地图数据、导航图连通性、典型路线规划和随机寻路性能，命令如下：

```bash
node scripts/test-navigation-system.js
```

（2）执行动态加权 A* 对比脚本，在统一网格、相同起终点和相同障碍条件下比较标准 A* 与动态加权 A*，命令如下：

```bash
node scripts/test-dynamic-pathfinding.js
```

（3）执行后端路径规划模块单元测试，验证网格创建、曼哈顿距离计算、可达路径、不可达路径和动态避障五类功能，命令如下：

```bash
cd backend
npx jest tests/pathPlanner.test.js --runInBand
```

上述测试脚本均来自本文系统工程目录，测试数据直接读取小程序端地图数据、导航图数据和后端路径规划模块，能够反映系统当前实现的实际运行状态。

### 6.3 真实项目数据验证（建议替换原 6.1.3 或新增）

首先使用 `scripts/test-navigation-system.js` 对系统实际地图数据进行验证。脚本运行结果显示，当前地图数据共包含 193 个要素，其中车位 168 个、墙体 14 个、入口 2 个、电梯 1 个、障碍物 3 个、交通标识 5 个。导航图包含 47 个节点和 54 条边，脚本检查结果显示所有节点均已连接，不存在孤立节点。

在典型路线测试中，本文选取“入口到出口”“入口到电梯”“电梯到出口”“A 区到 B 区”四组路线进行验证。实际运行结果如下表所示。

| 测试路线 | 是否找到路径 | 路径节点数 | 路径距离（脚本输出单位） |
| --- | --- | ---: | ---: |
| 入口到出口 | 是 | 18 | 2640.00 |
| 入口到电梯 | 是 | 9 | 1474.74 |
| 电梯到出口 | 是 | 9 | 1368.82 |
| A 区到 B 区 | 是 | 8 | 1286.80 |

在随机寻路测试中，脚本从 47 个导航节点中随机抽取起点和终点，连续执行 100 次路径规划。测试结果显示，100 次路径规划均成功完成，成功率为 100%，平均耗时为 0.12ms。该结果说明系统所构建的导航图具有较好的连通性，基础路径规划功能能够满足停车场内部导航的实时性要求。

### 6.4 动态加权 A* 对比实验（建议强化原 6.3.3）

为进一步验证动态加权 A* 算法的有效性，本文使用 `scripts/test-dynamic-pathfinding.js` 构造三类可控网格场景，分别为常规通行、狭窄绕行和多转弯场景。两种算法均在相同地图、相同起点、相同终点和相同障碍物条件下运行，记录原始路径长度、转弯次数、节点扩展数和计算时间。

| 场景 | 指标 | 标准 A* | 动态加权 A* |
| --- | --- | ---: | ---: |
| S1 常规通行 | 原始路径长度 | 35 | 35 |
| S1 常规通行 | 转弯次数 | 10 | 1 |
| S1 常规通行 | 节点扩展数 | 103 | 34 |
| S1 常规通行 | 计算时间(ms) | 1.905 | 2.065 |
| S2 狭窄绕行 | 原始路径长度 | 39 | 39 |
| S2 狭窄绕行 | 转弯次数 | 4 | 1 |
| S2 狭窄绕行 | 节点扩展数 | 105 | 38 |
| S2 狭窄绕行 | 计算时间(ms) | 1.641 | 1.174 |
| S3 多转弯场景 | 原始路径长度 | 71 | 71 |
| S3 多转弯场景 | 转弯次数 | 17 | 9 |
| S3 多转弯场景 | 节点扩展数 | 415 | 362 |
| S3 多转弯场景 | 计算时间(ms) | 2.334 | 6.544 |

从实验结果看，动态加权 A* 与标准 A* 在三组场景中的原始路径长度保持一致，说明改进算法没有破坏路径可达性。在路径质量方面，动态加权 A* 的转弯次数明显减少，S1 场景由 10 次降低到 1 次，S2 场景由 4 次降低到 1 次，S3 场景由 17 次降低到 9 次，说明路径平滑处理和增强代价模型能够提高驾驶友好性。在搜索效率方面，动态加权 A* 的节点扩展数也明显减少，表明动态权重策略能够减少无效搜索。计算时间方面，部分复杂场景下动态加权 A* 耗时略有增加，但整体仍处于毫秒级，能够满足停车场导航实时性要求。

需要说明的是，计算时间会受到电脑负载、Node.js 运行状态等因素影响，存在一定波动。因此本文更侧重使用路径长度、转弯次数和节点扩展数等稳定指标评价算法效果。

### 6.5 单元测试与模块验证（建议新增）

后端路径规划模块采用 Jest 进行单元测试，测试文件为 `backend/tests/pathPlanner.test.js`。该测试覆盖以下五类核心功能：

（1）网格地图是否能够正确创建；

（2）曼哈顿距离计算是否正确；

（3）在存在可达路径时是否能够成功返回路径；

（4）在障碍物完全阻断时是否能够正确返回不可达结果；

（5）新增障碍物后是否能够重新调整路径并避开障碍点。

实际执行命令如下：

```bash
npx jest tests/pathPlanner.test.js --runInBand
```

测试结果显示，`PathPlanner` 测试套件全部通过，共 5 个测试用例通过，0 个失败。该结果说明后端基础路径规划组件在核心场景下具有较好的正确性和稳定性。

## 四、建议删除或改写的内容

1. 将原文“导航节点：20 个、导航边：35 条”改为“导航节点：47 个、导航边：54 条”，否则与实际代码输出不一致。
2. 原表 6.4 的 P95、P99 数据目前缺少本次可运行证据。建议先不要写得过满，可改为“接口性能测试设计”，或者补跑可复现压测后再写。
3. “为了为了进一步说明”应改为“为了进一步说明”。
4. “动态加权A与标准A”应改为“动态加权 A* 与标准 A*”。
5. “动态加权A*算法的平均路径长度比传统A*和Dijkstra算法稍微好一点”表述偏口语，建议改为“动态加权 A* 在部分长距离、多转弯场景中表现出更优的路径质量，但其优势并非单纯体现在路径长度缩短，而主要体现在转弯次数减少、节点扩展数降低和驾驶友好性提升。”

## 五、研究结果简要说明

综合当前系统代码的实测结果可以看出，本文系统并非仅完成方案设计，而是已经通过真实项目数据和可控实验完成了功能验证。地图数据验证表明，系统当前包含 168 个车位、47 个导航节点和 54 条导航边，导航图连通性良好；典型路线测试中 4 组路线均成功生成，随机 100 次路径规划成功率达到 100%，平均耗时为 0.12ms，说明基础导航功能具备较好的可用性。动态加权 A* 对比实验进一步表明，在不增加原始路径长度的情况下，改进算法能够明显减少转弯次数和节点扩展数，使路径更符合停车场驾驶场景的实际需求。后端单元测试中 5 个核心用例全部通过，说明路径规划模块具有基本稳定性。上述结果共同证明，本文系统具备可运行、可复现、可验证的工程基础。
