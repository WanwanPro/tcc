# 论文非实证部分优化清单（0506）

本文档只针对“实证补全之外”的删除、改写和优化建议，重点关注本科毕业论文常见扣分点：表述口语化、逻辑过满、数据口径不一致、章节题目不准确、技术实现与论文主线脱节。

## 一、优先级最高：必须改

### 1. 摘要结论表述过满

原文：

> 实验结果表明，改进后的动态加权A*方法在路径长度、搜索效率和路径合理性上均优于传统A*算法。

问题：

当前实测结果显示，动态加权 A* 的主要优势是“转弯次数减少、节点扩展数降低、路径更平滑”。路径长度在三组可控实验中与标准 A* 保持一致，并不是明显更短。因此“路径长度、搜索效率和路径合理性上均优于”容易被认为过度结论。

建议改为：

> 实验结果表明，改进后的动态加权 A* 方法在保持路径可达性和路径长度基本稳定的基础上，能够减少转弯次数和节点扩展数，提高路径平滑性与驾驶友好性，能够较好满足智能停车场导航场景的实际应用要求。

### 2. 第 6.1.3 实验数据口径错误

原文：

> 车位数量：168个  
> 分区数量：3个  
> 导航节点：20个  
> 导航边：35条

问题：

当前代码实测为地图要素 193 个、车位 168 个、导航节点 47 个、导航边 54 条。论文中节点和边数量与代码不一致。

建议改为：

> 实验数据来源于系统小程序端实际使用的地图数据和导航图数据。经脚本验证，地图数据共包含 193 个要素，其中车位 168 个、墙体 14 个、入口 2 个、电梯 1 个、障碍物 3 个、交通标识 5 个；导航图包含 47 个节点和 54 条边，且节点连通性检查结果正常。

### 3. 第 6.3.4 系统接口性能表需要谨慎处理

原文：

> 表6.4  性能分析  
> 获取车位列表 45ms / 78ms / 120ms  
> 计算路径 12ms / 25ms / 45ms  
> 更新车位状态 35ms / 62ms / 95ms  
> 获取推荐车位 28ms / 52ms / 85ms

问题：

当前本机运行 `backend/tests/performance.test.js` 未通过，原因是 Jest 对 `../../shared/dal/atomic` 的解析失败。若没有其他压测记录支撑，P95、P99 数据不宜继续作为确定实测结果写入。

建议处理方式二选一：

（1）保守写法：删除 P95、P99 列，把表 6.4 改成“系统接口性能测试设计”，只说明测试目标和测试接口，不写未经复核的结果。

（2）补测写法：修复 Jest 路径解析或补充接口压测脚本，跑出真实数据后再写。若现在不准备修代码，建议先采用保守写法。

建议替代表述：

> 除算法测试外，本文还设计了系统接口性能测试，重点覆盖首页访问、车位列表查询和路径规划接口。测试目标为单次接口响应时间控制在 100ms 以内，10 个并发请求在 500ms 内完成。由于接口性能受数据库连接、运行环境和并发负载影响较大，本文将其作为系统工程可用性验证内容，不将单次运行数据作为算法性能结论。

### 4. “Dijkstra 对比”如果代码没有实际实现，应弱化

原文多处写：

> 比较动态加权A*、传统A*和Dijkstra算法在路径规划性能上的差异。

问题：

当前可运行脚本主要对比“标准 A*”与“动态加权 A*”。如果论文保留 Dijkstra，需要有对应代码、运行命令和结果来源；否则容易形成“写了三种算法，但只验证两种”的问题。

建议改法：

> 本实验主要比较标准 A* 与动态加权 A* 在相同网格场景下的路径规划差异，并结合文献和算法复杂度分析说明 Dijkstra 算法在大规模图搜索中的计算开销特点。

或者直接删除 Dijkstra 的实验表，只在相关研究或算法基础中作为理论背景出现。

## 二、建议删除或弱化的内容

### 1. 删除过多“未来可扩展接口”的重复表述

重复位置：

- 3.2 系统功能需求分析
- 3.3 系统总体架构设计
- 5.2 后端实现
- 5.4 系统接口设计与集成
- 7.2 研究不足与展望

问题：

“支付接口、车辆定位接口、车位传感器接口”等预留扩展被反复提到，但不是论文核心贡献。重复出现会稀释主线。

建议：

前文只保留一次，建议放在 3.3 或 5.4；其他章节删除或改成一句“系统保留后续扩展接口”即可。

### 2. 弱化“后台管理系统”篇幅

问题：

第 5.3 管理后台写得较长，包含仪表盘、公告、财务、统计、图表等内容。但你的论文题目和摘要主线是“车位引导与智能导航”，后台管理不是核心研究点。

建议：

保留后台作为系统完整性说明，但压缩“财务管理、收入统计、公告通知、导出数据”等业务细节。重点放在：

- 车位状态维护；
- 地图/节点数据管理；
- 小程序端车位状态同步；
- 管理端对导航和车位推荐的数据支撑。

建议把 5.3.4 改成“后台管理端关键支撑功能”，不要写得像一个后台管理系统需求文档。

### 3. 删除或减少口语化评价词

建议全文替换：

| 原表达 | 建议表达 |
| --- | --- |
| 稍微好一点 | 略有改善 / 在部分场景中表现更优 |
| 比较好 | 较好 / 具有一定优势 |
| 比较快 | 较快 |
| 可以 | 能够 |
| 完全可以满足 | 能够满足 / 基本满足 |
| 明显优于 | 在某些指标上优于 / 表现出优势 |
| 这种方式实现起来比较简单 | 该方法实现复杂度较低 |

## 三、病句和错字修改清单

### 1. A* 符号缺失

原文：

> A算法在寻找最短路径时会利用启发式函数  
> 普通A算法在停车场这类复杂环境中仍然存在一些不足  
> 针对传统A算法……

建议：

统一改为：

> A* 算法

### 2. “传A*算法”错字

原文：

> 动态加权A*算法在传A*算法基础上……

建议：

> 动态加权 A* 算法在传统 A* 算法基础上……

### 3. 重复词

原文：

> 为了为了进一步说明改进算法相较标准A*的优势……

建议：

> 为了进一步说明改进算法相较标准 A* 的优势……

### 4. 算法名缺星号

原文：

> 动态加权A与标准A均保持在毫秒级

建议：

> 动态加权 A* 与标准 A* 的计算时间均保持在毫秒级

### 5. 安全性部分病句

原文：

> 第三个请求是限制流量。  
> 四是指进行统一异常的处理。  
> 五包括日志和监控支持。

建议改为：

> 三是请求流量限制。系统分别对登录接口和普通接口设置请求频率限制，以降低暴力破解和恶意请求风险。四是统一异常处理。后端配置 404 处理中间件和全局错误处理中间件，前端对接口异常进行统一封装，使系统在发生错误时能够返回可读提示。五是日志与监控支持。系统保留访问日志和操作日志，便于后续问题追踪和运行维护。

### 6. 数据库实体句子缺顿号

原文：

> 主要包括管理员实体、停车场实体、停车位实体小程序用户实体、车辆实体……

建议：

> 主要包括管理员实体、停车场实体、停车位实体、小程序用户实体、车辆实体……

### 7. 图题疑似错误

原文：

> 见图5.15停车场用户实体图。

问题：

前文说的是“停车场实体”和“停车位实体”，图题却是“停车场用户实体图”，疑似命名错误。

建议：

> 见图 5.15 停车场与停车位实体图。

## 四、章节结构优化建议

### 1. 第 4 章标题建议更聚焦

当前：

> 4 核心算法设计与实现

建议：

> 4 动态加权 A* 路径规划与车位推荐方法

原因：

第 4 章不仅写路径规划，还写车位推荐与导航协同，改后更贴合实际内容。

### 2. 第 4.3 “算法实现与三种算法对比”建议调整

当前：

> 4.3 算法实现与三种算法对比

问题：

算法对比更适合放第 6 章实验部分，第 4 章应写方法实现。

建议：

> 4.3 动态加权 A* 算法实现

对比实验放到第 6 章。

### 3. 第 6 章标题建议强调“验证”

当前：

> 6 系统实验与性能分析

建议：

> 6 系统测试与实验验证

原因：

老师反馈重点是“看不到有没有做”，用“测试与实验验证”比“性能分析”更能回应质疑。

## 五、内容逻辑优化建议

### 1. 摘要不要说“均优于”

本科论文更稳妥的写法是“在某些关键指标上改善”。建议摘要结论聚焦三个实测指标：

- 路径可达性；
- 转弯次数；
- 节点扩展数；
- 平均耗时或毫秒级实时性。

### 2. 研究不足写得太弱，应更具体

当前“不足与展望”较泛，建议改成三点：

（1）真实部署范围不足：当前主要基于项目地图和离线脚本验证，尚未在真实停车场长周期运行。

（2）动态数据接入不足：车位状态和拥堵程度主要依赖系统模拟或后台维护，尚未接入真实传感器数据。

（3）推荐策略仍偏规则化：当前基于距离和偏好评分，未充分利用历史停车行为和区域热度预测。

这样写更符合本科论文“承认边界但不否定成果”的风格。

### 3. 相关研究部分要减少“万能结论”

比如：

> 这对提高智能停车场服务质量和系统运行效率有很大帮助。

建议改为：

> 相关研究表明，车位分配和负载均衡策略能够在一定程度上缓解停车资源分布不均问题，但其效果仍依赖实时数据质量和具体场景约束。

这样更学术、更稳。

## 六、建议保留的内容

以下内容不建议删：

1. 微信小程序端实现。它能体现系统实际完成度。
2. GPS 定位与位置服务。虽然室内导航不完全依赖 GPS，但该部分能支撑“入口匹配”和小程序实际使用流程。
3. 动态加权 A* 代价函数设计。它是论文核心创新点。
4. 车位推荐评分模型。虽然不是复杂模型，但能形成“推荐车位-路径规划-导航引导”的完整闭环。
5. 数据库设计。保留即可，但建议压缩 MongoDB 选型论证，避免篇幅过长。

## 七、最建议你现在改的 10 个点

1. 摘要中的“均优于传统 A*”改成“减少转弯次数和节点扩展数”。
2. 第 6.1.3 的节点数、边数改为当前代码实测值。
3. 删除或弱化 Dijkstra 的实验结果，除非补充可运行脚本。
4. 第 6.3.4 接口 P95/P99 表改成测试设计，或补测后再保留。
5. 全文统一 “A*” 写法，不要出现 “A算法”“动态加权A”。
6. 删除“为了为了”等明显错误。
7. 将“稍微好一点、比较好、完全可以”等口语化表达改成学术表达。
8. 压缩后台管理端的财务、公告、导出等非核心细节。
9. 把“不足与展望”改得更具体，承认真实部署和传感器接入不足。
10. 给第 6 章加“测试命令、脚本、输出摘要”，与实证补强稿合并使用。

